dataset_location: Data/spam_dataset.csv

Tokenization:
  oov_token: <OOV>
  vocab_size: 1000
  padding_type: post
  trunc_type: post

Model_Selection:
  test_size: 0.3

Model_Training:
  embedding_dim: 16
  loss: binary_crossentropy
  optimizer: adam
  metrics: ['accuracy']
  num_epochs: 30